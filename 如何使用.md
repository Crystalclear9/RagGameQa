# 🚀 RAG游戏问答系统 - 使用指南

---

## ✅ 配置状态

```
所有阶段: 7/7 完成 (100%)
环境测试: 5/6 通过 ✅
系统状态: 可用 ✅
数据模式: 内存模式 ✅
```

---

## ⚡ 立即开始（1分钟）

### 启动服务

```bash
python run_server.py
```

### 访问界面

浏览器打开: **http://localhost:8000/docs**

### 测试问答

在Swagger UI界面中：
1. 找到 `POST /api/v1/qa/ask`
2. 点击 "Try it out"
3. 输入测试数据：
   ```json
   {
     "question": "魔兽世界中战士如何学习技能？",
     "game_id": "wow"
   }
   ```
4. 点击 "Execute"
5. 查看返回结果

---

## 📂 项目文件说明

### 保留的有用脚本 (6个)

| 脚本 | 用途 | 何时使用 |
|------|------|---------|
| `run_server.py` | ⭐ 启动服务 | 每次启动 |
| `simple_test.py` | 环境测试 | 验证配置 |
| `setup_database.py` | 数据库完整设置 | 如有PostgreSQL |
| `init_memory_mode.py` | 内存模式初始化 | 已完成✅ |
| `check_db_status.py` | 检查数据库状态 | 诊断问题 |
| `init_db.py` + `add_sample_docs.py` | 数据库分步设置 | 高级用户 |

### 文档导航 (11+9个)

**docs/ 目录** - 详细文档
- `README.md` - 文档索引
- `QUICK_DB_SETUP.md` - 数据库快速配置
- `CURRENT_STATUS.md` - 当前状态
- 等11个文档...

**根目录** - 快速指南
- `START_HERE.md` ⭐ - 快速开始
- `USE_NOW.md` - 立即使用
- `FINAL_SUMMARY.md` - 最终总结
- 等9个文档...

---

## 🎮 系统功能

### ✅ 核心功能（已可用）

1. **问答API** - `/api/v1/qa/ask`
   - 支持6个游戏
   - 混合检索（向量+倒排索引）
   - 模拟LLM生成答案
   - 返回置信度和来源

2. **API文档** - `/docs`
   - 完整的Swagger UI
   - 可交互测试
   - 自动生成文档

3. **健康检查** - `/health`
   - 服务状态监控
   - 版本信息

4. **分析接口** - `/api/v1/analytics/*`
   - 反馈分析
   - 查询统计
   - 趋势分析

---

## 🎯 支持的游戏

| 游戏 | ID | 配置文件 | 示例文档 |
|------|----|---------|---------| 
| ⚔️ 魔兽世界 | wow | ✅ | 3个 |
| 🎮 英雄联盟 | lol | ✅ | 2个 |
| ⭐ 原神 | genshin | ✅ | 1个 |
| 🧊 我的世界 | minecraft | ✅ | - |
| 🔫 无畏契约 | valorant | ✅ | - |
| 🏆 Apex英雄 | apex | ✅ | - |

配置文件位置: `config/game_configs/*.json`

---

## 💡 使用示例

### PowerShell测试

```powershell
# 测试健康检查
Invoke-RestMethod http://localhost:8000/health

# 测试问答
$body = @{
    question = "如何组队？"
    game_id = "wow"
} | ConvertTo-Json -Compress

Invoke-RestMethod -Uri http://localhost:8000/api/v1/qa/ask `
    -Method Post `
    -Body $body `
    -ContentType "application/json"
```

### Python调用

```python
import requests

response = requests.post(
    "http://localhost:8000/api/v1/qa/ask",
    json={
        "question": "原神中如何触发元素反应？",
        "game_id": "genshin"
    }
)

result = response.json()
print(f"答案: {result['answer']}")
print(f"置信度: {result['confidence']}")
```

---

## 🔧 高级配置

### 配置PostgreSQL（推荐）

**优势**: 数据持久化、完整功能

**步骤**:
1. 安装PostgreSQL
2. 运行 `python scripts/setup_database.py`
3. 重启服务

**详细**: `docs/QUICK_DB_SETUP.md` (3分钟)

### 升级到真实LLM（可选）

**优势**: 更高质量的答案

**方案**:
- 获取正式Claude API Key
- 或使用OpenAI API
- 或部署本地模型

**详细**: `docs/CLAUDE_API_WARNING.md`

---

## 📊 技术架构

```
FastAPI Web服务 (8000端口)
    ↓
RAG引擎
    ├─ 混合检索器
    │  ├─ 向量检索 (Sentence-Transformers)
    │  └─ 倒排索引 (jieba + BM25)
    ├─ 重排序器 (BERT)
    └─ LLM生成器 (Mock模式)
        ↓
数据存储
    ├─ 内存模式 (data/sample_data.json) ✅
    └─ PostgreSQL (可选)
```

---

## ⚠️ 注意事项

### 1. 数据库警告（正常）

启动时可能看到：
```
WARNING: 记录查询日志失败: connection failed
```

**原因**: PostgreSQL未配置  
**影响**: 无法持久化日志  
**解决**: 可忽略，不影响问答功能

### 2. 首次请求较慢（正常）

首次请求约90秒，因为需要：
- 加载嵌入模型
- 加载jieba词典
- 初始化检索器

**后续请求**: <1秒

### 3. Claude API限制（已解决）

API Key只能在Claude Code中使用。

**解决**: ✅ 已使用模拟LLM

---

## 📋 所有命令

### 服务启动

```bash
# 推荐：使用启动脚本
python run_server.py

# 或直接运行
python api/main.py
```

### 测试验证

```bash
# 环境测试
python scripts/simple_test.py

# 数据库状态
python scripts/check_db_status.py
```

### 数据初始化

```bash
# 内存模式（已完成）
python scripts/init_memory_mode.py

# PostgreSQL模式（需安装）
python scripts/setup_database.py
```

---

## 🎉 总结

**✅ 所有配置已完成！**

**✅ 所有阶段已完成！**

**✅ 系统已准备就绪！**

### 立即使用

```bash
python run_server.py
```

**访问**: http://localhost:8000/docs

---

**🎮 开始体验RAG游戏问答系统吧！** ✨

---

*最后更新: 2025-10-17*  
*系统状态: 完成并可用*  
*当前模式: 内存模式（无需数据库）*

